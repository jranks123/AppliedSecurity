#include "std_helloworld.h"

int main( int argc, char* argv[] ) {
  int   r, x, y;

	printf("1");
      scanf( "%d",  &x );
      printf("2");
      scanf( "%d",  &y );

  r = x + y;

      printf( "%d\n",  r );





  return 0;
}
